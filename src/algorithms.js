const toExport = {};

// input: array 
// output: array of ids

// 12 most frequently listened songs out of the last 50
toExport.MFLlast50 = (arr) => {};

// write your custom algo here
toExport.custom1 = (arr) => {};

module.exports = toExport;
